model:
  input_size:
  hidden_size:
  num_layers:
  dropout:

training:
  epochs:
  batch_size:
  learning_rate:
  weight_decay:
  class_weights:

data:
  scaling_cols:
  target_col:
  stay_id_col:
  step_col:

artifacts:
  weights_path:
  scaler_path:
