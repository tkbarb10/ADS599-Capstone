random_state: 10

network:
  input_size:
  hidden_sizes:
  output_size:
  dropout:

training:
  epochs:
  batch_size:
  learning_rate:
  weight_decay:
  gamma:
  target_update_freq:
  cql_weight:

reward:
  base_reward:
  class_ratio:
  time_penalty:

replay_buffer:
  capacity:
  min_size:

data:
  test_size: 0.2
  batch_size: 16
  scaling_cols:
    - anchor_age
    - acuity
    - current_temperature
    - current_heartrate
    - current_resprate
    - current_o2sat
    - current_sbp
    - current_dbp
    - current_pain
    - current_pulse_pressure
    - current_map
    - height
    - weight
  state_cols:
  action_col:
  stay_id_col:
  step_col:
  p_icu_col:

artifacts:
  weights_path:
  scaler_path:
